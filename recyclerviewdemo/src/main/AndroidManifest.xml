<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="application.recyclerviewdemo">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".ui.RecyclerMainActivity" />
        <activity android:name=".ui.LinearLayoutRecyclerViewActivity" />
        <activity android:name=".ui.GridLayoutRecyclerViewActivity" />
        <activity android:name=".ui.StaggeredLayoutRecyclerViewActivity" />
        <activity android:name=".ui.ListViewActivity"></activity>
    </application>

</manifest>