<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="application.fourmodule">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".ui.FourModuleMainActivity" />
        <activity android:name=".ui.ServiceMainActivity" />
        <activity android:name=".ui.BroadcastMainActivity" />
        <activity android:name=".ui.ContentProviderMainActivity" />
        <activity android:name=".ui.contentprovider.ContactActivity" />

        <service
            android:name=".ui.service.MyService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>